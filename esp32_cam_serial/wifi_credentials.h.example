// wifi_credentials.h 템플릿. 이 파일을 wifi_credentials.h로 복사하고 값을 채우면 된다
// (wifi_credentials.h는 .gitignore에 등록되어 있어 커밋되지 않는다).
#pragma once

#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"
